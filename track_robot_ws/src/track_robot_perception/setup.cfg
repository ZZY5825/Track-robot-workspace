[develop]
script_dir=$base/lib/track_robot_perception
[install]
install_scripts=$base/lib/track_robot_perception

[tool:pytest]
# The host's vendored typeguard plugin targets a newer pytest API than ROS
# Foxy's system pytest. Project tests do not depend on that external plugin.
addopts = -p no:typeguard
