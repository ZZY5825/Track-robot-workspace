<!--
  Short-range supervised navigation using the minimal Foxy-compatible
  replanning pipeline. Automatic recovery, Spin, and BackUp are intentionally
  absent: a failed plan or controller aborts and leaves the safety chain
  disarmed until the operator explicitly retries.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <PipelineSequence name="NavigateWithReplanning">
      <RateController hz="1.0">
        <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
      </RateController>
      <FollowPath path="{path}" controller_id="FollowPath"/>
    </PipelineSequence>
  </BehaviorTree>
</root>
